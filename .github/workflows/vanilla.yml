name: Vanilla

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Run a one-line script
        run: echo Hello, world!

      - name: directory
        run: cd vanilla

      - name: install
        run: yarn install

      - name: build
        run: yarn run build
